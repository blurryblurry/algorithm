#include <iostream>
#include <stdio.h>
#define _CRT_SECURE_NO_WARNINGS
using namespace std;


int main()
{	
	int a, b=0, c=0,d=0;
	for (int i = 0; i <= 6; i++)
	{
		scanf_s("%d", &a);
		if (a < 10) b += a;
		else if (a >= 10 && a < 100) c += a;
		else d += a;

	}
	printf("%d %d %d", b, c, d);
}
