#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#define _CRT_SECURE_NO_WARNINGS
using namespace std;


int main()
{
	int n, sum = 0;
	cin >> n;

	for (int i = 1; i <= 6; i++)
	{
		if (n - i <= 6 && n - i > 0) printf("%d %d\n", i, n - i);
	}
	
	return 0;
}
