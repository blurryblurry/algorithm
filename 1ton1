#include <iostream>
#include <stdio.h>
#define _CRT_SECURE_NO_WARNINGS
using namespace std;


int main()
{	
	int a;
	int sum = 0;
	scanf_s("%d", &a);
	for (int i=1; i <= a; i++)
	{
		sum = sum + i;
		if (i == a)
		printf("%d", i);
		else
		printf("%d+",i);
	}

	printf("=%d", sum);
	

}
