#include <iostream>
#include <stdio.h>
#define _CRT_SECURE_NO_WARNINGS
using namespace std;


int main()
{	
	int a[7],index=0;
	cin >> a[0];
	int max = a[0];
	for (int i = 1; i <= 6; i++)
	{
		scanf_s("%d", &a[i]);
		if (max < a[i])
		{
			max = a[i];
			index = i;
		}
		

	}
	cout << index+1;
	
}
