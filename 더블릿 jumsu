#include <iostream>
using namespace std;

int main()
{
	int arr[100] = {}, sum1=0, sum2 = 0, n=1,sum=0;
	bool tf = true;
	cin >> n;
	for (int i = 0; i < n;i++)
	{
		scanf_s("%d", &arr[i]);
		
	}
	for (int i = 0; i < n; i++)
	{
		if (arr[i] == 1) sum1++;
		else sum1 = 0;
		sum2 += sum1;
	}
	
	
	printf("%d", sum2);
}
